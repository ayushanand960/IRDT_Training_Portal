# Documentation (Reference Only)

- Frontend: compiled build in `../frontend_dist/`.
- Backend: reference-only structure;
- Database: `../backend_partial/database/db.sqlite3` (snapshot).

This package is **not executable** and **not deployable**.
